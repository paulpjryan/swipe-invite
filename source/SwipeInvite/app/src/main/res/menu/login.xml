<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:compat="http://schemas.android.com/apk/res-auto" >

    <item android:id="@+id/login_button"
        android:icon="@drawable/ic_action_accept"
        android:title="Sign In"
        compat:showAsAction="withText|always" />

    <item android:id="@+id/new_user_button"
        android:icon="@drawable/ic_action_new"
        android:title="New User"
        compat:showAsAction="never" />

    <item android:id="@+id/action_forgot_password"
        android:title="@string/action_forgot_password"
        compat:showAsAction="never" />

</menu>
